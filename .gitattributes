# Normalise line endings to LF across platforms.
* text=auto eol=lf

# Scripts and wrappers must stay LF.
*.sh text eol=lf
gradlew text eol=lf
gradlew.bat text eol=crlf

# Treat these as binary.
*.jar binary
*.png binary
*.jpg binary
*.gif binary
*.ico binary

# Copy to .env and adjust. Never commit the real .env.
# These are consumed by docker-compose and (via the same names) the core-bank app.

DB_NAME=ledgerbank
DB_USERNAME=ledgerbank
DB_PASSWORD=ledgerbank
